---
name: Bug
about: Something behaves incorrectly
labels: ["bug"]
---

## Where it was seen

<!-- Date, environment (poligon / CI / local), build under test. Confirm you
     measured the binary you think you did (`strings` on a distinctive literal). -->

## What happens

<!-- Verbatim log lines, error strings and command output — never translated,
     never paraphrased. Point at the code with `file:line`. -->

## What should happen

## Reproduction

<!-- The shortest sequence that shows it, or "seen once, not reproduced" — that
     is an honest answer and is better than a guessed recipe. -->

## Acceptance criteria

- [ ] A regression test that fails before the fix and passes after it
- [ ]
